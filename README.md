# datasetgpt
DatasetGPT is a ChatGPT-style AI system that allows users to generate custom datasets using natural language. Instead of manually creating datasets or searching on platforms like Kaggle, users can simply type their requirements, and the system will automatically generate a dataset.
